import { initLazorkit } from '../sdk/lazorkit';

initLazorkit();

export const metadata = {
  title: '<%= appName %>',
  description: 'Generated by create-lazorkit-app',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}