<% if (features.includes('passkey') || features.includes('auth')) { %>import LoginForm from './features/LoginForm';<% } %>
<% if (features.includes('gasless')) { %>import TransferForm from './features/TransferForm';<% } %>
<% if (features.includes('pay-widget')) { %>import PayWidget from './features/PayWidget';<% } %>
<% if (features.includes('token-swap')) { %>import TokenSwap from './features/TokenSwap';<% } %>
<% if (features.includes('subscription')) { %>import SubscriptionService from './features/SubscriptionService';<% } %>

import './App.css'

function App() {
  return (
    <div className="main-container">
      <div className="header-section">
        <div className="header-content">
          <h1 className="main-title">Lazorkit CLI Wizard</h1>
          <p className="subtitle">Powered by Lazorkit SDK</p>
          <div className="header-accent"></div>
        </div>
      </div>

      <main className="features-grid">
<% if (features.includes('passkey')) { %>
        <div className="feature-card">
          <LoginForm />
        </div>
<% } %>
<% if (features.includes('gasless')) { %>
        <div className="feature-card">
          <TransferForm />
        </div>
<% } %>
<% if (features.includes('pay-widget')) { %>
        <div className="feature-card">
          <PayWidget amount="0.1" merchant="example-merchant" />
        </div>
<% } %>
<% if (features.includes('token-swap')) { %>
        <div className="feature-card">
          <TokenSwap />
        </div>
<% } %>
<% if (features.includes('subscription')) { %>
        <div className="feature-card">
          <SubscriptionService />
        </div>
<% } %>
      </main>

      <footer className="footer-section">
        <p>Built with Lazorkit SDK | Solana Mainnet Integration</p>
      </footer>
    </div>
  )
}

export default App